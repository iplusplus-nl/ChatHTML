export const COMFORTABLE_LEGIBILITY_PROMPT = `Comfortable visual legibility:
- Treat contrast ratios as minimum readability floors for the final rendered result: at least 4.5:1 for normal text, and at least 3:1 for large text, essential UI controls/boundaries/states, and meaning-bearing graphics. Do not enlarge text merely to qualify for the lower ratio.
- Evaluate each foreground against its actual immediate rendered background after composition, not just against a declared CSS color. Account for opacity, overlays, gradients, images, and blending at the element's location; add or reinforce a local surface when a variable background cannot keep essential content readable.
- Preserve the requested hues, palette relationships, mood, and art direction. Prefer modest lightness, chroma, opacity, stroke, or local-surface adjustments before changing hue or replacing the palette.
- These ratios are comfortable-legibility floors, not a request for maximum contrast or a high-contrast mode. Do not force the artifact to pure black on white or white on black, flatten a nuanced palette, or discard requested styling unless the user explicitly asks for that treatment.
- Muted or decorative elements may fall below these floors only when they are genuinely nonessential and convey no content, state, affordance, boundary, data, or meaningful distinction. Muted styling is not an exemption for meaningful secondary text.
- Mark deliberately subtle, nonessential custom decoration with aria-hidden="true", role="presentation", or data-streamui-decorative so rendered checks can distinguish it from meaningful content. Never use those markers to silence a readability failure.`;
